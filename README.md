# mynotess

## АААЭААЭАЭ

Вот один абзац.

А вот второй абзац.

```python
print(not(0))
print(not(1))
```

### ААЫЫЫЫЫЫ

```python
cursor.execute(f"SELECT * FROM users;")
all_users = {x["email"]: x for x in cursor.fetchall()}
```
